<snippet>
	<content><![CDATA[
getParticipantRegistry('${1:org.acme.Driver}')
	.then(function (${2:participantRegistry}) {
		return ${2}.getAll();
	});

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>getParticipantRegistry</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
</snippet>